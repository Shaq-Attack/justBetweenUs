import React, { ReactElement } from 'react';

const Read = (): ReactElement => {
    return (
        <div className="WebPage">
            <h1>Read</h1>
        </div>
    );
};

export default Read;
